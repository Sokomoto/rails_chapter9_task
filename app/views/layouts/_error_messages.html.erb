<div class="container">
	<div class="row">
		<% if @book.errors.any? %>
			<div class="alert alert-warnig">
				<h2 style="color: red"><%= pluralize(@book.errors.count,"error") %> prohibited this obj from being saved:</h2>
				<ul>
					<% @book.errors.full_messages.each do |message| %>
						<li style="color: red"><%= message %>
						</li>
					<% end %>
				</ul>
			</div>
		<% end %>
	</div>
</div>